#
# These things are run when an Openbox X Session is started.
# You may place a similar script in $HOME/.config/openbox/autostart
# to run user-specific things.
#

xsetroot -solid '#3366aa'

gkrellm &
artha &
parcellite &

# ugly hack - open pavucontrol and send it to desktop 8
pqp volume
(sleep 8 && xdotool set_desktop_for_window $(xdotool search --classname ^Pavucontrol$ | tail -n 1) 8) &

eval $(grep '^NAME=' /etc/os-release)
case $NAME in
    *BSD)
        # xmessage $NAME is '*BSD' &
        (sleep 0.8 && tint2) &
        (sleep 1.8 && dsbmixer -i) &
        ;;
    Gentoo)
        # xmessage $NAME is 'Gentoo' &
        (sleep 0.8 && fbpanel --profile default) &
        (sleep 1.8 && volumeicon) &
        (sleep 3.0 && conky -q -d) &
        ;;
    *)
        xmessage $NAME is 'unknown' &
        ;;
esac

(sleep 1.9 && $HOME/.fehbg) &
(sleep 2.0 && hello-fortune) &

(sleep 2.0 && picom) &

# Super key opens Openbox menu
# https://github.com/alols/xcape
xcape -e 'Super_L=Control_L|Menu'
